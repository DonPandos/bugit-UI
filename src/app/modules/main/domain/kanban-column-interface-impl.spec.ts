import { KanbanColumnInterfaceImpl } from './kanban-column-interface-impl';

describe('KanbanColumnInterfaceImpl', () => {
  it('should create an instance', () => {
    expect(new KanbanColumnInterfaceImpl()).toBeTruthy();
  });
});
